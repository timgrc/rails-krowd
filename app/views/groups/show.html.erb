<div id="wrapper">
  <%= render 'groups/dashboard/navbar' %>
  <div id="page-content-wrapper">
    <div class="container-fluid">
      <div class="row">
        <section id="general" class="margin-top">
          <%= render 'groups/dashboard/general' %>
        </section>
      </div>
      <div class="row">
        <section id="badges">
          <%= render 'groups/dashboard/badges' %>
        </section>
      </div>
      <div class="row">
        <section id="organization">
          <%= render 'groups/dashboard/organization' %>
        </section>
      </div>
<!--      <div class="row">
        <div class="col-xs-12">
          <section id="innovation">
            <%= render 'groups/dashboard/innovation' %>
          </section>
        </div>
      </div>
      <div class="row">
        <div class="col-xs-12">
          <section id="push">
            <%= render 'groups/dashboard/push' %>
          </section>
        </div>
      </div> -->
    </div>
  </div>
</div>

<% content_for(:after_js) do %>
  <script>
    if ( window.location.hash ) scroll(0,0);
    setTimeout( function() { scroll(0,0); }, 1);

    $(document).ready(function(){
      if(window.location.hash) {
          var anchor = window.location.hash
          $('.tab.active').removeClass("active");
          $('a[href="' + anchor + '"]').addClass("active");

          // smooth scroll to the anchor id
          $('html, body').animate({
              scrollTop: $(window.location.hash).offset().top - 70
          }, 1000, 'swing');
      }
    })
  </script>
<% end %>


<!-- Awards push post -->
<%= simple_form_for [@group, @push_post] do |f| %>
  <%= f.input :title %>
  <%= f.input :body %>
  <%= f.submit %>
<% end %>

<!-- Incentive push post -->
<%= simple_form_for [@group, @push_post] do |f| %>
  <%= f.input :title %>
  <%= f.input :body %>
  <%= f.submit %>
<% end %>
